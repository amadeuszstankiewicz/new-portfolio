
export default function Contact({contactRef}) {
    return (
        <div ref={contactRef} className="relative min-h-screen flex justify-center py-[64px] gap-5 container mx-auto">
            <div id="image-track" className="flex">
                contact
            </div>
        </div>
    )
}